---
description: Notifica o usuário via notify-send (Linux) ou Telegram ao concluir uma tarefa
agent: build
---

Tarefa concluída. Execute o seguinte comando shell para notificar o usuário:

!`.opencode/commands/notify.sh "$ARGUMENTS"`
